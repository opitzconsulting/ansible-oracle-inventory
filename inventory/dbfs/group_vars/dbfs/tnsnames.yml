---
# Example configuration
oracle_tnsnames_config:
  orclpdb:
    alias:
      - orclpdb_dbsnmp
      - orclpdb_checkmk
    connect:
      service_name: orclpdb
    address:
      - host: localhost

tnsnames_installed:
  - home: db19-si-ee
    tnsname: orclpdb
    state: present
